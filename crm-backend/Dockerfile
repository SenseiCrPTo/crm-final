# Шаг 1: Используем официальный легковесный образ Node.js v18
FROM node:18-alpine

# Шаг 2: Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Шаг 3: Копируем package.json и package-lock.json для установки зависимостей
# Это делается отдельно для эффективного кэширования
COPY package*.json ./

# Шаг 4: Устанавливаем зависимости проекта
RUN npm install

# Шаг 5: Копируем все остальные файлы проекта (server.js, bot.js, папку public и т.д.)
COPY . .

# Шаг 6: Сообщаем Docker, что наше приложение работает на порту 3001
EXPOSE 3001

# Шаг 7: Команда для запуска приложения при старте контейнера
CMD ["npm", "run", "start"]